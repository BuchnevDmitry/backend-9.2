spring:
  application:
    name: tool

  datasource:
    url: jdbc:postgresql://localhost:5433/${TOOL_DB_NAME}
    username: ${TOOL_USERNAME}
    password: ${TOOL_PASSWORD}
    driverClassName: org.postgresql.Driver

server:
  port: 8085

logging:
  config: classpath:log4j2-plain.xml


